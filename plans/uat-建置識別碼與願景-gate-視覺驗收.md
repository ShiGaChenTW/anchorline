# UAT: 建置識別碼與願景 gate 視覺驗收

**建立時間：** 2026-08-15 11:41
**最後更新：** 2026-08-15 11:41
**狀態：** 進行中

> **目的：**驗收本輪三項變更的**畫面**——這三項的程式邏輯閘門（tsc／1145 例單元測試）都已綠，但 Interceptor 擴充功能資料面卡死，agent 沒有任何畫面證據。這份清單就是那些 `[DEFERRED-VERIFY]` 的兌現。
>
> **測試編號：**UAT-20260815-02
>
> **本輪內容：**W3-1b 狀態列建置識別碼（`e8e3e00`）· W3-3 願景欄位進 gate warn 級（`389b7a5`）· 抽單 fail-open 修復（`3ee1f6f`）
>
> **前置（一次就好）：**
> 1. 在 repo 根目錄執行 `bun run app` 重新 build
> 2. `rm -rf /Applications/Anchorline.app && cp -R src-tauri/target/release/bundle/macos/Anchorline.app /Applications/`
> 3. 執行 `git log -1 --format=%h` 記下短雜湊（第 2 題要比對它）
> 4. `open -a Anchorline`
>
> **已通過免測：**tsc 綠、`bun test` 1145 例綠（基準 1126，W3-3 +5、W3-1b +14，零迴歸）、`bunx vite build` 綠、注入值已從 `dist/assets/status-bar-*.js` 產物確認為建置期字面量而非執行期查 git。這份清單只驗**畫面**與**真安裝版**的行為。
>
> **注意：**這份與 `plans/uat-下一版實作驗收（wave1+2）.md` 是兩份不同的報告，那份的 10 題仍未測。前置的重新 build 會讓兩份都跑在同一個新 build 上。

> 在 Anchorline 的 Task Tracking 開這一份逐題勾選。「失敗」與「不測」必須填說明。

## T1 狀態列出現建置識別碼 <!-- anc:t=PWQER4SZ -->

**流程：**
1. 開啟 Anchorline，登入後停在任一頁
2. 看視窗最底部的狀態列
3. 看狀態列的最右側（時鐘的右邊）

**預期：**
最右側有一串像 `1.1.0 · 757422e · 08-15 11:31` 的文字（版本 · 短雜湊 · 月-日 時:分），三段用 ` · ` 分隔，單行不換行

**結果：** 未測

**說明：**
（無）

## T2 識別碼的雜湊與剛裝的 build 相符 <!-- anc:t=821FS399 -->

**流程：**
1. 把狀態列那串文字中間那段短雜湊記下來
2. 回到終端機執行 `git log -1 --format=%h`
3. 比對兩個值

**預期：**
兩者相同。若雜湊後面有一個 `+`，代表 build 當下工作區有未 commit 的變更——那也是正確行為，把 `+` 去掉後比對應相同

**結果：** 未測

**說明：**
（無）

## T3 識別碼沒有擠掉使用者名稱 <!-- anc:t=XRPN3YS9 -->

**流程：**
1. 看狀態列右側，同時找使用者名稱與剛才那串識別碼
2. 確認兩者都完整顯示、沒有被截斷成「…」

**預期：**
使用者名稱與建置識別碼並存，都完整可讀，沒有任何一方被切掉

**結果：** 未測

**說明：**
（無）

## T4 識別碼可以用滑鼠拖曳選取 <!-- anc:t=JP2293BG -->

**流程：**
1. 把游標移到狀態列那串識別碼的最左邊
2. 按住左鍵往右拖曳到那串文字的最右邊
3. 放開左鍵

**預期：**
文字被反白選取（可以再按 ⌘C 複製）。狀態列其他部分（例如時鐘）拖不動是正常的，只有這串要能選

**結果：** 未測

**說明：**
（無）

## T5 點一下識別碼會複製到剪貼簿 <!-- anc:t=8K0H3MPP -->

**流程：**
1. 用滑鼠左鍵點一下狀態列那串識別碼
2. 看狀態列是否跳出訊息
3. 到任一可輸入的地方按 ⌘V

**預期：**
狀態列跳出複製成功的訊息，且貼上的內容就是那串識別碼

**結果：** 未測

**說明：**
（無）

## T6 視窗縮窄後識別碼仍看得到 <!-- anc:t=M8XMK8EB -->

**流程：**
1. 把 Anchorline 視窗的寬度拖到很窄（大約螢幕的三分之一，或比 900 像素更窄）
2. 看狀態列最右側

**預期：**
建置識別碼仍然看得到，沒有整串消失、沒有跟其他元素疊在一起

**結果：** 未測

**說明：**
（無）

## T7 願景欄位留空會出提醒但擋不住送審 <!-- anc:t=RMBAV1M8 -->

**流程：**
1. 開任一專案的 PRD 編輯台
2. 找到「01 三行摘要」裡的「專案功能說明與願景」欄位
3. 把該欄位的內容全部清空
4. 看 gate 檢查結果的區塊
5. 看送審按鈕

**預期：**
出現一則**提醒等級（warn）**的項目，文字告訴你要先寫 2–3 句敘事說明主要目的；**送審按鈕仍然可以按**，沒有被擋住

**結果：** 未測

**說明：**
（無）

## T8 只寫敘事沒有條列會出第二種提醒 <!-- anc:t=Y1SH1095 -->

**流程：**
1. 在同一個「專案功能說明與願景」欄位，貼上一段夠長的純敘事（至少三、四句話，不要用任何 - 或 • 開頭的條列）
2. 點一下欄位外面讓它存檔
3. 看 gate 檢查結果的區塊

**預期：**
剛才那則「太單薄」的提醒消失，改出現另一則提醒，內容是建議在敘事之後用 `-`、`•` 或 `1.` 開頭逐條列出主要功能與目標

**結果：** 未測

**說明：**
（無）

## T9 願景提醒的文字沒有被截斷 <!-- anc:t=GH7JF0A3 -->

**流程：**
1. 維持上一題的狀態（有願景提醒顯示中）
2. 仔細讀那則提醒的完整文字

**預期：**
提醒的說明文字完整顯示，沒有被切掉或變成「…」，讀得出來該補什麼

**結果：** 未測

**說明：**
（無）

## T10 敘事加條列兩則提醒都消失 <!-- anc:t=MD6Y2FPM -->

**流程：**
1. 在「專案功能說明與願景」欄位，敘事後面另起一行加上兩三行以 `- ` 開頭的條列
2. 點一下欄位外面讓它存檔
3. 看 gate 檢查結果的區塊

**預期：**
跟願景有關的提醒全部消失，不再有任何一則

**結果：** 未測

**說明：**
（無）

## T11 下一版規劃書的主題渲染 <!-- anc:t=PVC5AJRW -->

**流程：**
1. 在 Finder 或終端機開啟 repo 裡的 `docs/next-version-plan.html`（用瀏覽器開）
2. 整份由上往下捲一遍
3. 把系統或瀏覽器切換成深色模式，重新整理該頁

**預期：**
淺色與深色兩種模式下文字都讀得到、沒有白底白字或黑底黑字；表格沒有超出畫面；標題與內文的層次分得出來

**結果：** 未測

**說明：**
（無）
