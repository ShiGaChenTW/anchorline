---
name: Bug
about: 有東西壞了
labels: bug
---

## 發生什麼

## 怎麼重現

1.
2.

## 環境

- OS 與版本：
- App 版本：
- `openspec --version`（有裝的話）：
- `gh --version`（有裝的話）：

## 如果跟 CLI 有關

App 設定裡的「CLI 路徑」顯示什麼？（找不到 `openspec` / `gh` 多半是
GUI 進程的 PATH 問題，見 `docs/BRIDGE.md` §5）
