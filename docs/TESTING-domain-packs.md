# 手動測試流程 — 領域包

> 對象：`Anchorline Test.app`（測試變體，資料與正式版分開存，不會互相污染）

---

## 🔁 第二輪：只需要測這 5 段（其餘已關閉）

第一輪你回報了三個 bug，都修好了；另外有兩條是我的測試文件寫錯地方造成的假陰性。
**下面 5 段是還需要你動手的**，其他我已經用 Playwright／App 內量測關掉了。

| # | 測什麼 | 為什麼還要你測 | 章節 |
|---|---|---|---|
| **R1** | 寫作教練現在拉得動了 | flex 壓縮修掉了，但「手感」只有你能判斷 | §1 補測 |
| **R2** | 領域 gate 逐條顯示 | 上次看錯地方（要看**右欄「結構 gate」卡**，不是底部狀態列） | §2 |
| **R3** | 自動重掃：新增檔案 → 重開 App 會自己出現 | 需要真的關掉再開，腳本模擬不了 | §4 |
| **R4** | 存檔會真的寫進資料夾 | 改走原生寫檔了（原本用瀏覽器下載，WKWebView 靜默無事） | §5a · §5e |
| **R5** | AI 產出不再被誤判 | 修了「模型在前面多寫一段話」的情況，需要真金鑰跑一次 | §5b–§5d |

### 這一輪修了什麼（對應你回報的）

| 你回報的 | 根因 | 現況 |
|---|---|---|
| 寫作教練文字太多會卡住拉不動 | `.coach-body` 是直向 flex，子項預設被**壓縮**而不是溢出 → `scrollHeight` 永遠等於 `clientHeight` → 捲軸從不出現 | 已修。量到 `scrollHeight` 644 → 1403，滾輪實際捲動 600px |
| 「下載空白範本」「下載 .md」沒反應 | WKWebView 沒有下載管理員，`<a download>` 靜默無事 | 改走原生寫檔（新增 `write_domain_pack`，檔名在 Rust 端驗證） |
| AI 產出「缺少 --- frontmatter ---」但有內文 | 模型在前面加了「好的，以下是…」 | 改成從第一行單獨的 `---` 開始挖 |
| 「沒有看到阻擋項目」（§2 §4） | **不是 bug** —— 我寫「看右下角」是錯的，那裡只有摘要行 | 文件已改。實測 digital_account 12 條 finding、2 條 block 都在 |

### 我已經關掉的（不用再測）

- §2 阻擋項確實存在 —— Playwright 實測 12 條 finding
- §6 全部四條回歸：通用 gate 規則不變、匯出 MD 含領域章節內文、新建精靈有領域下拉（5 個選項）、
  總覽各專案分別計算（wealth 11 章/4 擋、digital_account 10 章/6 擋、generic 7 章/4 擋）
- 三個領域切換無 JS 錯誤

### 還沒有人測過的（我也測不到）

- **§4 同名覆寫**：我在 App 內用注入快取的方式驗過機制，但沒用真的檔案走一次
- **§4 壞檔不拖垮其他包**：同上，機制驗過，沒用真檔
- **§6 送審 → 審閱 → 簽核**：跨頁流程，需要人點

---

## 🧪 R1–R5 逐步流程

### R1 — 寫作教練拉得動了

1. 開 **編輯工作台**，左欄「領域」下拉選 **財管 / 投資**（章節最多，11 章）
2. 右欄「寫作教練」把 **檢查清單 / 結構 gate / AI 助教 / 好例子** 四張卡**全部展開**
3. 滑鼠移到右欄，滾輪往下滾

- [ ] 內容會跟著捲動，可以一路看到最底下的「好例子」
- [ ] 「結構 gate」卡裡的清單**完整**，沒有斷在半行
- [ ] 左欄章節清單（11 個）也捲得動

> 🐞 又卡住的話，告訴我你當時選的領域與展開了哪幾張卡。

---

### R2 — 領域 gate 逐條顯示

**看哪裡**：右欄「寫作教練」→ **「結構 gate」** 那張卡。
畫面最底下狀態列只有摘要行（「N 項阻擋」），不會列出是哪幾條 —— 上次就是看錯這裡。

1. 領域切到 **數位帳戶 / 數位銀行**
2. 展開「結構 gate」卡

- [ ] 清單裡有 **✗ KYC 缺風險等級對應** 與 **✗ 未對應個資法第 27 條**
- [ ] 也有 warn 的 **! 交易監控缺可量化門檻**、**! STR 通報要點不足**
3. 點 08 章「KYC / AML 合規要點」→ 在「身分驗證流程」貼：
   `第二類帳戶非臨櫃開戶，依風險等級對應差異化驗證`
- [ ] 「KYC 缺風險等級對應」變成綠色 **✔ KYC 風險等級已對應**
4. 點 09 章 → 在「安全維護措施」貼：`依個資法第 27 條建立安全維護計畫`
- [ ] 「未對應個資法第 27 條」也轉綠

---

### R3 — 自動重掃

前置：先做完 §4 前半（已指定領域包資料夾）。

1. **偏好設定**（齒輪或 `⌘,`）→ 左側 **品質檢查** → 找到 **🏷️ 領域包**
2. 勾選「**開 App 時自動重掃資料夾**」→ 應出現 toast

3. 到 `examples/domain-packs/` 複製一份 `insurance.md` 成 `test2.md`，
   把裡面的 `name: insurance` 改成 `name: test2`、`displayName` 改成「測試領域二」，存檔
4. **完全結束 App**（`⌘Q`，不是關視窗）再開

- [ ] 編輯台的領域下拉多了「測試領域二」

5. 回設定頁**取消勾選**，再複製一份成 `test3.md`（`name: test3`），`⌘Q` 重開

- [ ] 「測試領域三」**沒有**自己出現
- [ ] 按「重新讀取」之後才出現

> 這條要真的 `⌘Q` —— 只關視窗不算，程序還活著就不會重跑載入流程。

---

### R4 — 存檔會真的寫進資料夾

上一輪這兩顆按了完全沒反應（WKWebView 沒有下載管理員）。現在改走原生寫檔。

1. 偏好設定 → 領域包 → 展開「**用 AI 產生一個領域包**」
2. 點「**下載空白範本**」

- [ ] 出現 toast「已存到 …/domain-pack-template.md」
- [ ] **去資料夾確認那個檔真的在**，打開來是註解完整的範本

3. （做完 R5 產出之後）點「**下載 .md**」

- [ ] toast 顯示存到哪，檔名是包的 `name`
- [ ] 檔案真的在資料夾裡

> 如果還沒指定過領域包資料夾，它會先跳資料夾選擇器 —— 那是預期行為。
> 🐞 toast 說成功但檔案不在，那是嚴重問題（等於資料靜默遺失）。

---

### R5 — AI 產出

**先確認連線通了**（上一輪這裡也是壞的）：

1. 偏好設定 → **AI 工具** → 你的端點 `https://api.anthropic.com/v1/` 不用改
2. 按「**測試連線**」

- [ ] 不再是「網路請求失敗」
  〔修了兩件事：CSP 原本擋掉所有外部連線；端點結尾的 `/` 會疊成 `/v1/v1/messages`〕

**然後產生**：

3. 偏好設定 → 品質檢查 → 領域包 → 展開「用 AI 產生一個領域包」
4. 貼這段描述 → 點「產生」

```
台灣的線上證券下單 App。必須涵蓋證券商管理規則的委託人身分確認、
逐筆交易的洗錢防制監控、以及當沖與信用交易的風險預告書。
交易糾紛多半出在成交回報與委託紀錄對不起來。
```

- [ ] 狀態列顯示「已產生「…」（N 個章節）」——**不再是**「缺少 --- frontmatter ---」
- [ ] 出現「第一次驗證未過，已自動修正」也算通過，那代表重試機制在做事

5. 在「要改哪裡？」輸入 `把當沖那條改成 warn，另外加一段成交回報對帳` → 點「依指示重產」

- [ ] 產出更新，看得出照著指示改了

6. 在產出的文字框裡，把某條規則的 `section:` 改成一個不存在的名字 → 點「**加入我的領域**」

- [ ] 紅字「還不能加入：規則 … 指向不存在的章節 …」
- [ ] 改回去再點一次 → 成功，且 toast 說寫進資料夾
- [ ] 回編輯台，領域下拉選得到它，章節與 gate 都在

> 🐞 如果又出現解析失敗，**把文字框裡的原始內容整段貼給我** —— 那是唯一能重現的東西。

---

## 0. 安裝與啟動

**已安裝好：`/Applications/Anchorline Test.app`**（quarantine 屬性已移除，直接雙擊即可，不必右鍵）

它與你平常用的 `Anchorline.app` 是**兩個獨立的 App**：


|                         | 識別碼                       | localStorage key           |
| ----------------------- | ------------------------- | -------------------------- |
| Anchorline.app（你原本的）    | `dev.anchorline.app`      | `anchorline:state:v6:prod` |
| Anchorline Test.app（這次） | `dev.anchorline.app.test` | `anchorline:state:v6:test` |


測壞了不影響正式資料，也不會蓋掉原本的 App。要移除就直接把
`/Applications/Anchorline Test.app` 丟到垃圾桶。

> 重建：`VITE_APP_VARIANT=test bunx tauri build --config '{"productName":"Anchorline Test","identifier":"dev.anchorline.app.test"}'`

登入：使用者選 **Scott（管理員）**，密碼 `demo`。

- [ ] App 開得起來，看得到專案清單

> ⚠️ 這是**測試變體**，localStorage key 是 `anchorline:state:v6:test`，
> 跟你平常用的 `Anchorline.app`（`:prod`）完全分開。測壞了不影響正式資料。

---

## 1. 內建領域切換（最基本的一條）

進 **編輯工作台**，左欄「PRD 章節」上方有「領域」下拉。

- [x] 下拉有 5 個選項：通用 / 數位帳戶 / 信貸 / 支付 / 財管（**沒有** `_base`、`_template`）
- [x] 選「數位帳戶」→ 章節從 7 個變 **10 個**，多出 08 KYC/AML、09 個資保護、10 主管機關申報
- [x] 底部狀態列的「結構檢查」數字跟著變（阻擋數會增加）
- [x] 選「財管」→ **11 個**章節（財管多一段，費用與風險揭露分開寫）

**換回去不能掉字（最重要的一條）**

- [x] 在「數位帳戶」的 KYC 章節隨便寫幾個字
- [x] 切回「通用」→ 章節剩 7 個，且**出現灰色提示**「1 個章節的內容不屬於目前領域。內容仍保留…」
- [x] 切回「數位帳戶」→ 剛才寫的字**原封不動還在**，灰色提示消失

> 🐞 如果字不見了，那是嚴重 bug，請告訴我。

---

## 2. 領域 gate 會擋，也會放

**看哪裡**：右欄「寫作教練」→ 展開「**結構 gate**」那張卡。逐條的阻擋項在那裡；
畫面最底下狀態列的「結構檢查：N 項阻擋」只是**摘要行**，不會列出是哪幾條。
（上一輪回報「沒有看到阻擋項目」就是看錯地方 + 清單被捲動 bug 裁掉，已修。）

留在「數位帳戶」，展開右欄的「結構 gate」卡。

- [ ] 阻擋項裡有 **KYC 缺風險等級對應**、**未對應個資法第 27 條 &gt; 沒有看到阻擋項目**
- [ ] 在 08 章「身分驗證流程」寫入含「第二類帳戶」或「風險等級」的字 → 那條阻擋消失，轉為通過 **&gt; 沒有看到阻擋項目**
- [ ] 在 09 章「安全維護措施」寫入含「個資法第 27 條」的字 → 第二條阻擋也消失 **&gt; 沒有看到阻擋項目**

> 🐞 只會擋不會放的 gate 是壞的。如果寫對了還擋著，記下你寫的內容告訴我。

---

## 3. 本機教練（不需要 API Key）

**先確認偏好設定裡沒有填 AI 金鑰**（或先清掉），這一段測的是離線那條路。

- [x] 在「數位帳戶」點 08 KYC 章節，右欄「寫作教練」有內容，且標示【本機規則檢查】
- [x] 空白時會列出「KYC 缺風險等級對應」「交易監控缺可量化門檻」「STR 通報要點不足」
- [x] 填好之後那些警告轉成正面的「KYC 風險等級已對應」

> 這一條是我這輪補的缺口 —— 之前沒 API Key 的話，領域章節的教練欄是空的。

---

## 4. 自帶領域包（桌面才有，這是主菜）

素材已經準備好，選資料夾時指這裡（可直接複製貼上）：

```
~/orca/workspaces/Project_Anchorline/Fintech-PRD-Agent導入/examples/domain-packs
```

裡面有 `insurance.md`（可直接用的保險領域包）與 `_template.md`（空白範本）。

> 在 macOS 的選擇資料夾對話框按 `⌘⇧G` 可以直接貼路徑。

進 **偏好設定 → 🏷️ 領域包**。

- [x] 「選擇領域包資料夾…」按鈕**不是灰的**（桌面版才會亮；瀏覽器版是灰的）
- [x] 點它 → 選 `examples/domain-packs/` → 出現 toast「已載入 N 個自訂領域」
- [x] 清單多一列「**保險 / 保代** · insurance · 自訂」
- [x] `_template` **不會**出現在清單裡（`_` 開頭是內部保留）
- [x] 狀態列顯示「自訂資料夾：…/examples/domain-packs」

**它真的能用嗎**

- [x] 回編輯台，領域下拉多了「保險 / 保代」
- [x] 選它 → 多出 08 保單條款要點、09 招攬與告知義務
- [ ] 結構檢查出現 **不保事項不足**、**未寫契約撤銷權**、**未對應保險法第 148 條之 1** 三條阻擋 **&gt; 沒有看到阻擋項目**
- [x] 在「不保事項」貼三行以 `-` 開頭的條目 → 第一條阻擋消失

**同名覆寫**（機制我驗過，但沒用真檔走過一次）

- [ ] 用文字編輯器打開 `examples/domain-packs/_template.md`，把 `name: _template` 改成 `name: generic`、`displayName` 改成「通用（我改過的）」，存檔
- [ ] 回設定頁按「**重新讀取**」→ 清單裡 generic 那列標成「**自訂（覆寫內建）**」
- [ ] 編輯台的下拉顯示「通用（我改過的）」
- [ ] 改回去、再按一次「重新讀取」→ 回復成「內建」

**自動重掃選項  &gt; 請你幫忙測試**

- [ ] 勾「開 App 時自動重掃資料夾」→ toast 確認
- [ ] 在資料夾裡**新增**一個 `.md`（複製 insurance.md 改個 `name` 就好）
- [ ] 完全結束 App 再開 → 新的領域**自己出現了**
- [ ] 取消勾選 → 再新增一個檔 → 重開 App **不會**自動出現，按「重新讀取」才出現

**壞檔不能拖垮其他包  &gt; 請你幫忙測試**

- [ ] 在資料夾放一個 `broken.md`，內容就寫一行 `hello`
- [ ] 按「重新讀取」→ 其他領域**照常運作**，清單下方用紅色列出 `broken.md` 與失敗原因

> 🐞 如果一個壞檔讓整個領域清單消失，那是 bug。

- [ ] 「清除自訂」→ 確認對話框 → 自訂領域消失，只剩 5 個內建；**已經寫過的專案內容不會被刪**

---

## 5. AI 協作產出領域包 ← **R4 · R5**

需要先在 **偏好設定 → AI 寫作教練模型與金鑰設定** 填好金鑰並儲存
（Gemini / OpenAI / Anthropic / 本機 Ollama 皆可）。

展開 **偏好設定 → 領域包 → 「用 AI 產生一個領域包」**。

**5a 範本下載**

- [ ] 點「下載空白範本」→ 瀏覽器下載 `domain-pack-template.md`  &gt; 點擊按鈕沒用
- [ ] 打開它，內容是註解完整的範本（四種 predicate 都有示範） &gt; 下載沒啟動,所以沒測試到

**5b 產生**

- [x] 描述欄留白或只打三個字 → 點「產生」→ 出現紅字「描述太短…」，**不會**發出請求  
- [x] 沒填金鑰時點「產生」→ 紅字提示先去設定金鑰，**不會**發出請求
- [x] 貼下面這段描述 → 點「產生」

```
台灣的線上證券下單 App。必須涵蓋證券商管理規則的委託人身分確認、
逐筆交易的洗錢防制監控、以及當沖與信用交易的風險預告書。
交易糾紛多半出在成交回報與委託紀錄對不起來。
```

- [x] 按鈕變灰、狀態列顯示「產生中…」
- [ ] 產出區出現，狀態列顯示「已產生「…」（N 個章節）」 &gt; 出現“產生失敗：修正後仍無法解析：AI 產出：缺少 --- frontmatter ---”,但有產出內文
- [ ] 如果顯示「第一次驗證未過，已自動修正」——**那是正常的**，代表重試機制有作用 &gt; 沒有出現

**5c 迭代**

- [x] 在「要改哪裡？」輸入：`把當沖那條改成 warn，另外加一段成交回報對帳`
- [ ] 點「依指示重產」→ 產出更新，且能看出照著指示改了 &gt; 出現“產生失敗：修正後仍無法解析：AI 產出：缺少 --- frontmatter ---”

**5d 手改後仍受檢**

- [ ] 直接在產出的文字框裡，把某條規則的 `section:` 改成一個不存在的名字 &gt; 出現“產生失敗：修正後仍無法解析：AI 產出：缺少 --- frontmatter ---”
- [ ] 點「加入我的領域」→ 紅字「還不能加入：規則 … 指向不存在的章節 …」 &gt; 出現“產生失敗：修正後仍無法解析：AI 產出：缺少 --- frontmatter ---”
- [ ] 改回去 → 再點「加入我的領域」→ 成功 &gt; 無法測試

**5e 落地  &gt; 請你幫忙測試**

- [ ] 若前面已指定過領域包資料夾 → toast 說「已寫入領域包資料夾：xxx.md」，且**該檔真的出現在資料夾裡**
- [ ] 若沒指定資料夾 → toast 說「只存在這台瀏覽器，建議下載後放進資料夾」
- [ ] 點「下載 .md」→ 檔名是包的 `name` &gt; 沒有反應
- [ ] 回編輯台 → 下拉選得到這個新領域，章節與 gate 都在

> 🐞 5e 那條「寫進資料夾」很重要：如果只留在記憶體，開了自動重掃之後
> 下次開 App 它就會消失 —— 那是最難查的一種資料遺失。

---

## 6. 回歸（大部分已由 Playwright 關閉）

- [x] 通用領域的專案，結構檢查行為跟以前一樣 — 實測仍有 Non-Goals 與指標規則，共 6 條 finding
- [ ] **送審 → 審閱頁 → 簽核流程正常** ← 只剩這條要人點
- [x] 匯出 MD 含得到領域章節的內容 — 實測含「KYC / AML」標題與內文
- [x] 專案總覽的每個專案顯示各自的阻擋數 — wealth 11 章/4 擋 · digital_account 10 章/6 擋 · generic 7 章/4 擋
- [x] 新建專案 →「進階設定」有「領域」下拉（5 個選項，含數位帳戶）

---

## 回報格式

有問題的話，把這三樣給我就夠：

1. 哪一條 checkbox
2. 你實際看到什麼（截圖最好）
3. 如果是 AI 產出的問題，把文字框裡的**原始內容**貼給我 —— 那是可重現的關鍵

