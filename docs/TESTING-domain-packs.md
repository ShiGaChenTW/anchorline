# 手動測試流程 — 領域包

> 對象：`Anchorline Test.app`（測試變體，資料與正式版分開存，不會互相污染）
> 目的：補上自動化測試蓋不到的部分 —— **桌面才有的資料夾存取**，以及**設定頁的 UI 接線**

自動化已經蓋住的不用再手測：規則直譯（46 條）、領域包解析與繼承（64 條）、
章節骨架／標記分離（12 條）、AI 產出的驗證閘門與重試迴圈（16 條）。
下面每一項都是**只有真的點下去才知道**的。

---

## 0. 安裝與啟動

```
產物：src-tauri/target/release/bundle/macos/Anchorline Test.app
安裝：已複製到 /Applications/
```

**未簽章，第一次開會被 Gatekeeper 擋。** 兩種開法：

- 在 Finder 對著 App **按右鍵 → 打開** → 對話框再按一次「打開」
- 或 `xattr -dr com.apple.quarantine "/Applications/Anchorline Test.app"` 後正常開啟

登入：使用者選 **Scott（管理員）**，密碼 `demo`。

- [ ] App 開得起來，看得到專案清單

> ⚠️ 這是**測試變體**，localStorage key 是 `anchorline:state:v6:test`，
> 跟你平常用的 `Anchorline.app`（`:prod`）完全分開。測壞了不影響正式資料。

---

## 1. 內建領域切換（最基本的一條）

進 **編輯工作台**，左欄「PRD 章節」上方有「領域」下拉。

- [ ] 下拉有 5 個選項：通用 / 數位帳戶 / 信貸 / 支付 / 財管（**沒有** `_base`、`_template`）
- [ ] 選「數位帳戶」→ 章節從 7 個變 **10 個**，多出 08 KYC/AML、09 個資保護、10 主管機關申報
- [ ] 底部狀態列的「結構檢查」數字跟著變（阻擋數會增加）
- [ ] 選「財管」→ **11 個**章節（財管多一段，費用與風險揭露分開寫）

**換回去不能掉字（最重要的一條）**

- [ ] 在「數位帳戶」的 KYC 章節隨便寫幾個字
- [ ] 切回「通用」→ 章節剩 7 個，且**出現灰色提示**「1 個章節的內容不屬於目前領域。內容仍保留…」
- [ ] 切回「數位帳戶」→ 剛才寫的字**原封不動還在**，灰色提示消失

> 🐞 如果字不見了，那是嚴重 bug，請告訴我。

---

## 2. 領域 gate 會擋，也會放

留在「數位帳戶」，看右下角「結構檢查」。

- [ ] 阻擋項裡有 **KYC 缺風險等級對應**、**未對應個資法第 27 條**
- [ ] 在 08 章「身分驗證流程」寫入含「第二類帳戶」或「風險等級」的字 → 那條阻擋消失，轉為通過
- [ ] 在 09 章「安全維護措施」寫入含「個資法第 27 條」的字 → 第二條阻擋也消失

> 🐞 只會擋不會放的 gate 是壞的。如果寫對了還擋著，記下你寫的內容告訴我。

---

## 3. 本機教練（不需要 API Key）

**先確認偏好設定裡沒有填 AI 金鑰**（或先清掉），這一段測的是離線那條路。

- [ ] 在「數位帳戶」點 08 KYC 章節，右欄「寫作教練」有內容，且標示【本機規則檢查】
- [ ] 空白時會列出「KYC 缺風險等級對應」「交易監控缺可量化門檻」「STR 通報要點不足」
- [ ] 填好之後那些警告轉成正面的「KYC 風險等級已對應」

> 這一條是我這輪補的缺口 —— 之前沒 API Key 的話，領域章節的教練欄是空的。

---

## 4. 自帶領域包（桌面才有，這是主菜）

素材已經準備好：**`<專案資料夾>/examples/domain-packs/`**，裡面有
`insurance.md`（可直接用的保險領域包）與 `_template.md`（空白範本）。

進 **偏好設定 → 🏷️ 領域包**。

- [ ] 「選擇領域包資料夾…」按鈕**不是灰的**（桌面版才會亮；瀏覽器版是灰的）
- [ ] 點它 → 選 `examples/domain-packs/` → 出現 toast「已載入 N 個自訂領域」
- [ ] 清單多一列「**保險 / 保代** · insurance · 自訂」
- [ ] `_template` **不會**出現在清單裡（`_` 開頭是內部保留）
- [ ] 狀態列顯示「自訂資料夾：…/examples/domain-packs」

**它真的能用嗎**

- [ ] 回編輯台，領域下拉多了「保險 / 保代」
- [ ] 選它 → 多出 08 保單條款要點、09 招攬與告知義務
- [ ] 結構檢查出現 **不保事項不足**、**未寫契約撤銷權**、**未對應保險法第 148 條之 1** 三條阻擋
- [ ] 在「不保事項」貼三行以 `-` 開頭的條目 → 第一條阻擋消失

**同名覆寫**

- [ ] 用文字編輯器打開 `examples/domain-packs/_template.md`，把 `name: _template` 改成 `name: generic`、`displayName` 改成「通用（我改過的）」，存檔
- [ ] 回設定頁按「**重新讀取**」→ 清單裡 generic 那列標成「**自訂（覆寫內建）**」
- [ ] 編輯台的下拉顯示「通用（我改過的）」
- [ ] 改回去、再按一次「重新讀取」→ 回復成「內建」

**自動重掃選項**

- [ ] 勾「開 App 時自動重掃資料夾」→ toast 確認
- [ ] 在資料夾裡**新增**一個 `.md`（複製 insurance.md 改個 `name` 就好）
- [ ] 完全結束 App 再開 → 新的領域**自己出現了**
- [ ] 取消勾選 → 再新增一個檔 → 重開 App **不會**自動出現，按「重新讀取」才出現

**壞檔不能拖垮其他包**

- [ ] 在資料夾放一個 `broken.md`，內容就寫一行 `hello`
- [ ] 按「重新讀取」→ 其他領域**照常運作**，清單下方用紅色列出 `broken.md` 與失敗原因

> 🐞 如果一個壞檔讓整個領域清單消失，那是 bug。

- [ ] 「清除自訂」→ 確認對話框 → 自訂領域消失，只剩 5 個內建；**已經寫過的專案內容不會被刪**

---

## 5. AI 協作產出領域包 ← **這段是這次唯一沒實機驗過的**

需要先在 **偏好設定 → AI 寫作教練模型與金鑰設定** 填好金鑰並儲存
（Gemini / OpenAI / Anthropic / 本機 Ollama 皆可）。

展開 **偏好設定 → 領域包 → 「用 AI 產生一個領域包」**。

**5a 範本下載**

- [ ] 點「下載空白範本」→ 瀏覽器下載 `domain-pack-template.md`
- [ ] 打開它，內容是註解完整的範本（四種 predicate 都有示範）

**5b 產生**

- [ ] 描述欄留白或只打三個字 → 點「產生」→ 出現紅字「描述太短…」，**不會**發出請求
- [ ] 沒填金鑰時點「產生」→ 紅字提示先去設定金鑰，**不會**發出請求
- [ ] 貼下面這段描述 → 點「產生」

```
台灣的線上證券下單 App。必須涵蓋證券商管理規則的委託人身分確認、
逐筆交易的洗錢防制監控、以及當沖與信用交易的風險預告書。
交易糾紛多半出在成交回報與委託紀錄對不起來。
```

- [ ] 按鈕變灰、狀態列顯示「產生中…」
- [ ] 產出區出現，狀態列顯示「已產生「…」（N 個章節）」
- [ ] 如果顯示「第一次驗證未過，已自動修正」——**那是正常的**，代表重試機制有作用

**5c 迭代**

- [ ] 在「要改哪裡？」輸入：`把當沖那條改成 warn，另外加一段成交回報對帳`
- [ ] 點「依指示重產」→ 產出更新，且能看出照著指示改了

**5d 手改後仍受檢**

- [ ] 直接在產出的文字框裡，把某條規則的 `section:` 改成一個不存在的名字
- [ ] 點「加入我的領域」→ 紅字「還不能加入：規則 … 指向不存在的章節 …」
- [ ] 改回去 → 再點「加入我的領域」→ 成功

**5e 落地**

- [ ] 若前面已指定過領域包資料夾 → toast 說「已寫入領域包資料夾：xxx.md」，且**該檔真的出現在資料夾裡**
- [ ] 若沒指定資料夾 → toast 說「只存在這台瀏覽器，建議下載後放進資料夾」
- [ ] 點「下載 .md」→ 檔名是包的 `name`
- [ ] 回編輯台 → 下拉選得到這個新領域，章節與 gate 都在

> 🐞 5e 那條「寫進資料夾」很重要：如果只留在記憶體，開了自動重掃之後
> 下次開 App 它就會消失 —— 那是最難查的一種資料遺失。

---

## 6. 回歸（確認沒弄壞既有功能）

- [ ] 通用領域的專案，結構檢查行為跟以前一樣（Non-Goals ≥ 3、指標要有數字…）
- [ ] 送審 → 審閱頁 → 簽核流程正常
- [ ] 匯出 MD 含得到領域章節的內容
- [ ] 專案總覽的每個專案顯示各自的阻擋數（不同領域的專案數字不一樣）
- [ ] 新建專案 →「進階設定」裡有「領域」下拉，選了之後建出來的專案就是那個領域

---

## 回報格式

有問題的話，把這三樣給我就夠：

1. 哪一條 checkbox
2. 你實際看到什麼（截圖最好）
3. 如果是 AI 產出的問題，把文字框裡的**原始內容**貼給我 —— 那是可重現的關鍵
