/**
 * UAT 報告格式的**預設規格文件**。
 *
 * 單一真相在磁碟：`~/.anchorline/uat-format.md`。出題的 agent（Uat skill）
 * 出題前會讀那份檔；設定頁編輯的也是那份檔 —— 所以「App 存檔、skill 同步」
 * 不需要任何複製機制，兩邊讀的本來就是同一份。
 *
 * 這裡的常數只有一個用途：磁碟上還沒有那份檔時，設定頁用它當種子。
 * 改預設值改這裡；改「現行格式」去設定頁或直接改磁碟上那份。
 */
export const UAT_FORMAT_PATH = "~/.anchorline/uat-format.md";

export const UAT_FORMAT_DEFAULT = `# UAT 報告格式規格

> 這份文件給**出題的 agent** 讀。改了這裡，之後產出的實測報告就照新規則走。
> 方言的機器層（`+ "`## 題目`" + ` 區段、`+ "`anc:t=`" + ` 錨點、結果詞彙）由程式定義，
> 這裡管的是**內容品質與檔頭脈絡** —— 不要在這裡改結構性語法。

## 檔頭脈絡（spec 的 context 欄位，序列化成 blockquote）

出題時把下列欄位寫進 context（有就帶，沒有就省略，不要編造）：

- **專案：** 專案名
- **日期/時間：** 出題當下
- **目的：** 這一輪在驗什麼（含 build hash／release 或 dev）
- **已通過免測：** 前輪已過、本輪不重測的項目與依據
- **測試次數：** 第幾輪；有前輪紀錄就附路徑
- **測試編號：** UAT-YYYYMMDD-NN
- **前置（一次就好）：** 開始測之前要做的環境準備，寫成編號步驟收在引用區內
  （**不要**寫成 \`## 前置\` 區段 —— 那會被當成一題）

## 出題品質門檻

- **流程必須條列式**：一步一行、一個原子動作，spec 的 steps 陣列一步一元素；
  禁止用「→」把多步串成一句
- **流程可重現**：照著做就能做出來，不寫「測試結帳」這種摘要
- **預期可判定**：看得出通過或失敗，不寫「運作正常」
- **一題一行為**：混兩個行為的題拆開
- 題目排序照使用者操作動線（主幹先通），不照程式碼結構
- 題數對齊改動範圍：小改 3–6 題，整頁功能 8–15 題，不灌水

## 結果詞彙（機器層，僅供參考，不可在此修改）

未測 / 通過 / 失敗 / 不測 / 暫時跳過 —— 「失敗」與「不測」必須填說明。
`;
